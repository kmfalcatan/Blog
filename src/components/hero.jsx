import "../assets/css/hero.css";

function Hero({ darkMode }) {
  return (
    <div className={`heroContainer ${darkMode ? "dark" : "light"}`}>
      <p>das</p>
    </div>
  );
}

export default Hero;
